# Assets

| File | Purpose |
|------|---------|
| `eslint.config.js` | ESLint flat config (root) with TODO ticket rule. |
| `eslint-rules/todo-ticket-ref.js` | Custom rule: TODO comments must reference a ticket. |
| `package.json.snippet.json` | Example devDependencies and scripts (versions are illustrative). |

Copy `eslint.config.js` and `eslint-rules/todo-ticket-ref.js` to the project; merge script and dependency entries from the snippet into the project `package.json`.
