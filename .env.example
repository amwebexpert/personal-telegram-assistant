# Telegram bot token from BotFather
TELEGRAM_BOT_TOKEN=

# Tavily API key from https://app.tavily.com
TAVILY_API_KEY=

# Path to Google OAuth credentials JSON downloaded from Google Cloud Console
# (same file used for both Calendar and Gmail)
GOOGLE_OAUTH_CREDENTIALS_PATH=/path/to/gcp-oauth.keys.json
